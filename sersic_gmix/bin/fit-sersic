#!/usr/bin/env python
import sys
import sersic_gmix

def get_hlr(n):
    if n < 1:
        n=1
    if n > 4:
        n=4

    # 20.0 for n=1
    # 40.0 for n=4
    hlr = 20.0 + (n-1.0)*20.0/3.0
    return hlr

n=float(sys.argv[1])
hlr=get_hlr(n)
ngauss=4

sersic_gmix.fit_sersic(n, hlr, ngauss)
