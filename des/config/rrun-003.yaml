# redmapper 6.2.12 randoms, no erase, with ra,dec cuts
lcat_vers: "rcat-003"

# ngmix009 lensfit, pz v0.1, skynet_mag_auto
scat_vers: "scat-001"

# this is used in xshear, not for the maskflags
healpix_nside: 64

# only applies if using point photoz
#zdiff_min: 0.2
