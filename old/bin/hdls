#!/usr/bin/env python

import sys
from optparse import OptionParser

parser=OptionParser(__doc__)

parser.add_option("-h",action="store_true",dest="human_readable",
                  default=False,
                  help="Print human readable sizes")

def ls(pattern):
    command="hadoop fs -ls %s" % pattern


def lsall(patterns):
    res=[]
    for pattern in patterns:
        run_ls
def main():

    options, args = parser.parse_args(sys.argv[1:])
    if len(args) < 1:
        parser.print_help()
        sys.exit(45)

    for 

if __name__=="__main__":
    main()
