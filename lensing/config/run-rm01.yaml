run:           "rm01"
lens_sample:   "rm01"
src_sample:    "05"

# for healpix
nside:         4096

comment: | 

    lenses:

        Red mapper "redmapper-dr8-3.4-nord", cut to z [0.08,0.4]
        Includes the strict 1Mpc edge cut.

        nbin 21

        r min max [0.02,36.567]

    sources:
        src r < 21.5.  Uses mean scinv as a function of lens redshift
        has scinv on grid [0.02,0.41], dz 0.015
